# name: ArgoCD Sync Trigger

# on:
#   push:
#     branches:
#       - main  # PR 머지 시 동기화 트리거

# jobs:
#   sync-argocd:
#     runs-on: ubuntu-latest
#     steps:
#       - name: ArgoCD Sync
#         run: |
#           curl -X POST https://your-argocd-server/api/v1/applications/your-app/sync \
#                -H "Authorization: Bearer ${{ secrets.ARGOCD_TOKEN }}"
